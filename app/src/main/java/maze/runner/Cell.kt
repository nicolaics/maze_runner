package maze.runner

class Cell(var appearance : Int,
           var hasRight : Boolean,
           var hasLeft : Boolean,
            var hasTop : Boolean,
            var hasBottom : Boolean) {
}